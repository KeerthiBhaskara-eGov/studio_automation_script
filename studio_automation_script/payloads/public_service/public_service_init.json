{
  "service": {
    "tenantId": "{{tenantId}}",
    "businessService": "{{service}}",
    "module": "{{module}}",
    "status": "ACTIVE",
    "additionalDetails": {
      "note": "{{note}}"
    }
  }
}